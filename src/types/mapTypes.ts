export interface LocationData {
  latitude: number;
  longitude: number;
  title: string;
}

export interface Region {
  latitude: number;
  longitude: number;
  latitudeDelta: number;
  longitudeDelta: number;
}
